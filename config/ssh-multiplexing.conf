# OpenSSH Connection Multiplexing Configuration
# Shares a single TCP connection for multiple concurrent SSH sessions to the same host.

Host *
    ControlMaster auto
    ControlPath ~/.ssh/sockets/%r@%h:%p
    ControlPersist 15m
    ServerAliveInterval 60
    ServerAliveCountMax 3
    TCPKeepAlive yes
    Compression yes
